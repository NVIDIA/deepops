apiVersion: v1
kind: Namespace
metadata:
  name: cuda-optimized-fastspeech
  labels:
    name: cuda-optimized-fastspeech
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cuda-optimized-fastspeech
  namespace: cuda-optimized-fastspeech
  labels:
    app: cuda-optimized-fastspeech
spec:
  selector:
    matchLabels:
      app: cuda-optimized-fastspeech
  template:
    metadata:
      labels:
        app: cuda-optimized-fastspeech
    spec:
      containers:
        - name: cuda-optimized-fastspeech
          image: registry.local:31500/cuda-optimized-fastspeech
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8888
          resources:
            limits:
              nvidia.com/gpu: 1
          args:
            - jupyter
            - lab
            - --ip=0.0.0.0
            - --no-browser
            - --allow-root
            - --port=8888
            - --NotebookApp.token=''
            - --NotebookApp.password=''
            - --NotebookApp.allow_origin='*'
            - --NotebookApp.base_url=/
          volumeMounts:
            - mountPath: /dev/shm
              name: dshm
      volumes:
        - name: dshm
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.service.nodeport.port: "30888"
    kompose.service.type: nodeport
  creationTimestamp: null
  labels:
    app: cuda-optimized-fastspeech
  name: cuda-optimized-fastspeech
  namespace: cuda-optimized-fastspeech
spec:
  ports:
    - name: jupyterlab
      nodePort: 30888
      port: 8888
      targetPort: 8888
  selector:
    app: cuda-optimized-fastspeech
  type: NodePort
status:
  loadBalancer: {}
