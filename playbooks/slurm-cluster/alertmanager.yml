---
- name: Install docker
  include_tasks: ../container/docker.yml

- hosts: "{{ hostlist | default('all') }}"
  become: yes
  tasks:
    - name: configure alertmanager
      include_role:
        name: alertmanager

