---
- name: Install docker
  include_tasks: ../container/docker.yml

- hosts: "{{ hostlist | default('all') }}"
  become: yes
  tasks:
    - name: configure prometheus
      include_role:
        name: prometheus
