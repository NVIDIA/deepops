---
batch_connect:
  template: "basic"
script:
  native:
    - "--gpus=<%= bc_num_gpus.blank? ? {{ ood_codeserver_app_def-gpus }} : bc_num_gpus.to_i %>"
    - "--cpus-per-gpu={{ ood_codeserver_app_cpus-per-gpu }}"
    - "--mem-per-gpu={{ ood_codeserver_app_mem-per-gpu }}"
